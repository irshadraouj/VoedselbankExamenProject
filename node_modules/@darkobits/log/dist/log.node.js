import env from "@darkobits/env";
import merge from "deepmerge";
import { createCommonLogger } from "./lib/log.common.js";
function createLogger(options = {}) {
  return createCommonLogger(merge({
    // Set the default log level to the LOG_LEVEL environment variable.
    level: env("LOG_LEVEL"),
    // Set the default debug expression to the DEBUG environment variable.
    debugExpression: env("DEBUG")
  }, options));
}
function createLoggerFactory(defaultOptions = {}) {
  return (options = {}) => createLogger(merge(defaultOptions, options));
}
export {
  createLogger,
  createLoggerFactory
};
//# sourceMappingURL=log.node.js.map
