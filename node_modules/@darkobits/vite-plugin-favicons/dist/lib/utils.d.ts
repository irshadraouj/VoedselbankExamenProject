import { type FaviconResponse } from 'favicons';
import type { EmittedViteFile, FaviconsPluginOptions } from '../etc/types';
import type { HtmlTagDescriptor } from 'vite';
/**
 * Provided a list of EmittedFile descriptors and a search query, returns the
 * single EmittedFile whose name most closely matches the search query.
 */
export declare function findEmittedFile(emittedFiles: Array<EmittedViteFile>, searchFilePath: string): EmittedViteFile | undefined;
/**
 * Provided a FaviconsPluginOptions object, generates assets and returns a
 * FaviconResponse object.
 */
export declare function generateFavicons(opts: FaviconsPluginOptions): Promise<FaviconResponse>;
/**
 * Provided an array of EmittedFile objects and an array of HTML strings
 * returned by `favicons`, updates the href attributes in each <link> node to
 * contain the resolved file name produced by Vite rather than the original
 * input filename. Returns an array of HtmlTagDescriptor objects that may be
 * returned by `transformHtml`.
 *
 * See: https://vitejs.dev/guide/api-plugin.html#transformindexhtml
 */
export declare function parseHtml(emittedFiles: Array<EmittedViteFile>, fragments: Array<string>, base?: string): HtmlTagDescriptor[];
/**
 * Provided a list of EmittedFile objects from Vite and an Android or Firefox
 * manifest's contents, updates each icon's source to reflect the final name
 * generated for the file by Vite.
 */
export declare function updateManifest(emittedImages: Array<EmittedViteFile>, manifestContents: string): string;
