(() => {
  const root = document.querySelector('.leveranciers');
  if (!root) return;

  // De leverancierspagina haalt zijn data van de ExpressionEngine channel; hier is op dit front-end moment niets extra's nodig.
})();
